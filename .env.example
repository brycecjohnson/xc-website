# AWS Configuration
BUCKET_NAME=skyview-xc-team
AWS_REGION=us-east-1
CLOUDFRONT_DISTRIBUTION_ID=your-distribution-id-here
CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net

# Note: AWS credentials should be configured via AWS CLI
# Run: aws configure
# Never commit AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY to the repo